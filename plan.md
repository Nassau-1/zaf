1. **Explore codebase and task requirements:** Review `dashboard/server.js` for command injection vulnerabilities involving `execSync`. Identify specific lines: 326, 1547, 1582, 1597, 1642, 1668, 1669, 1767, 1883.
2. **Import `execFileSync`:** Add `execFileSync` to the `child_process` import at the top of the file.
3. **Refactor `git clone` (lines 1767, 1883):** Replace `execSync(\`git clone --depth 1 "${url}" "${tmpDir}"\`, ...)` with `execFileSync('git', ['clone', '--depth', '1', url, tmpDir], ...)` to pass arguments safely.
4. **Refactor `git config` (lines 1668, 1669):** Replace `execSync(\`git config --global user.name "${name}"\` ...)` with `execFileSync('git', ['config', '--global', 'user.name', name], ...)` and similar for `email`. Remove `.replace(/['"]/g, '')` as it's no longer necessary to prevent quote breakout since we are passing arguments directly.
5. **Refactor `git clone`, `git init`, `git remote add` (lines 1547, 1582, 1597):** Replace `execSync` with `execFileSync` passing arguments as an array.
6. **Refactor `node parse.js` (line 326):** Replace `execSync` with `execFileSync(process.execPath, [PARSE_SCRIPT, '--repos-root', REPOS_ROOT], ...)`.
7. **Refactor custom harnesses `versionCmd` (line 1642):** Validate `versionCmd` against shell metacharacters before executing, OR use `execFileSync` with parsed arguments. Since Windows handles `.cmd` vs `.exe` awkwardly without `shell: true`, we will parse `versionCmd` into command and arguments, and use `execFileSync` with `shell: false`. On Windows, we'll try `execFileSync` without shell, which is safer. Actually, `execSync` with a regex check `/[&|;`$()<>]/` is safer and more robust cross-platform for a config-provided string, but to follow "properly passed as an argument list", I'll use `execFileSync` splitting by space. Wait, `git` commands are where the user inputs arbitrary URLs, so `execFileSync` is perfect. For `versionCmd`, I'll split by space and pass to `execFileSync(cmd, args, { timeout: 8000, shell: process.platform === 'win32' })`. Wait, `shell: true` with args on Windows is still vulnerable if an arg contains `&`. So I'll just use `execSync` but add strict validation to `versionCmd` OR I'll split it and use `shell: false` and accept it might require `.cmd` on Windows. Wait, `execFileSync` on Windows handles `.exe` automatically, but not `.cmd`. If `CLI_HUB_VERSION_CMDS` is `'claude --version'`, and it's a `.cmd`, it will fail. So I'll add `shell: process.platform === 'win32'` but ALSO validate `versionCmd` to prevent injection.
8. **Test the changes:** Start the server and run testing scripts to ensure no functionality is broken and command injection is fixed.
9. **Pre-commit checks:** Run `pre_commit_instructions` tool to complete testing, verification, review, and reflection.
10. **Submit PR:** Commit and push the changes as `🛡️ Sentinel: [CRITICAL] Fix command injection in execSync`.
